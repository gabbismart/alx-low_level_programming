#include <stdio.h>
/**
 * main - print the phrase with proper grammer,
 * but the outcome is a piece of art,
 * Return: 0 if exited properlu, non-zero otherwise
 */
int main
(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
